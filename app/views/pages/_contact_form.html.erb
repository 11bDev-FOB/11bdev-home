<%= form_with model: contact, url: contacts_path, local: true, 
             data: { turbo: true }, 
             class: "space-y-6",
             id: "contact_form" do |form| %>
             
  <div>
    <h2 class="text-3xl font-bold text-dark-olive mb-6">Send Us a Message</h2>
    <p class="text-gray-600 mb-6">
      Ready to start your mission? Fill out the form below and we'll get back to you within 24 hours.
    </p>
  </div>
  
  <div class="grid md:grid-cols-2 gap-6">
    <div>
      <%= form.label :name, "Name *", class: "block text-sm font-semibold text-dark-olive mb-2" %>
      <%= form.text_field :name, 
                         placeholder: "Your full name",
                         required: true,
                         class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-olive focus:border-olive transition-colors" %>
      <% if contact.errors[:name].any? %>
        <p class="text-red-500 text-sm mt-1"><%= contact.errors[:name].first %></p>
      <% end %>
    </div>
    
    <div>
      <%= form.label :email, "Email *", class: "block text-sm font-semibold text-dark-olive mb-2" %>
      <%= form.email_field :email, 
                          placeholder: "your.email@example.com",
                          required: true,
                          class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-olive focus:border-olive transition-colors" %>
      <% if contact.errors[:email].any? %>
        <p class="text-red-500 text-sm mt-1"><%= contact.errors[:email].first %></p>
      <% end %>
    </div>
  </div>
  
  <div>
    <%= form.label :phone, "Phone", class: "block text-sm font-semibold text-dark-olive mb-2" %>
    <%= form.telephone_field :phone, 
                           placeholder: "(555) 123-4567",
                           class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-olive focus:border-olive transition-colors" %>
    <p class="text-gray-500 text-sm mt-1">Optional - for faster response times</p>
  </div>
  
  <div>
    <%= form.label :message, "Message *", class: "block text-sm font-semibold text-dark-olive mb-2" %>
    <%= form.text_area :message, 
                      placeholder: "Tell us about your project, timeline, and any specific requirements...",
                      rows: 6,
                      required: true,
                      class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-olive focus:border-olive transition-colors resize-none" %>
    <% if contact.errors[:message].any? %>
      <p class="text-red-500 text-sm mt-1"><%= contact.errors[:message].first %></p>
    <% end %>
  </div>
  
  <div class="flex items-center justify-between">
    <%= form.submit "Send Message", 
                   class: "btn-military cursor-pointer",
                   data: { disable_with: "Sending..." } %>
    
    <p class="text-sm text-gray-500">
      We typically respond within 24 hours
    </p>
  </div>
  
  <div class="text-sm text-gray-500">
    <p>
      * Required fields | By submitting this form, you agree to our 
      <a href="#" class="text-muted-blue hover:text-olive transition-colors">privacy policy</a>.
    </p>
  </div>
<% end %>